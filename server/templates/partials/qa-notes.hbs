<!-- QA Notes Section -->
<div class="mb-8">
    <h2 class="text-xl font-semibold mb-4 text-muted-foreground">QA Notes</h2>
    <div class="space-y-4">
        {{#each emailData.qaNotes}}
        <div class="border border-gray-200 rounded p-2 bg-blue-50">
            <div class="text-sm text-muted-foreground whitespace-pre-wrap">
                {{text}}
            </div>
        </div>
        {{/each}}
    </div>
</div>